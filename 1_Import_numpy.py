# if you don't have numpy then download
# command: pip install numpy
# and then import numpy in the file

import numpy as np      # create agile 'np' using keyword as
print(np.__version__)       # now check the version of numpy
